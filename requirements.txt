# NVIDIA NeMo ASR framework (main branch includes streaming inference
# support and the CUDA graph fix for cudaStreamGetCaptureInfo)
nemo_toolkit[asr] @ git+https://github.com/NVIDIA/NeMo.git@main

# Audio processing (pinned to 2.8.x — torchaudio 2.9+ has a hard dependency
# on torchcodec which requires a matching PyTorch ABI build)
torch==2.8.0
torchaudio==2.8.0
pydub==0.25.1
librosa==0.11.0
soundfile==0.13.1

# Data handling
pandas==2.3.3
boto3==1.40.45

# System monitoring (optional)
psutil==7.1.0

# CUDA Python bindings (required for streaming inference CUDA graphs)
cuda-python==12.8.0

# For audio format conversion
ffmpeg-python==0.2.0
